using Sieve.Services;

namespace Application.Common.Sieve.CustomSortings
{
    // todo: add new sortings according to new grid at FE
    // if many custom sortings, make this class partial
    public class AppSortMethods : ISieveCustomSortMethods
    {
        //public IQueryable<Shop> CustomSortName(IQueryable<Shop> source, bool useThenBy, bool desc)
        //{
        //    var result = desc
        //        ? source.OrderByDescending(p => p.Id)
        //        : source.OrderBy(p => p.Id);

        //    return result;
        //}

    }
}